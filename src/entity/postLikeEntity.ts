export interface PostLikeEntity {
  userId: string;
  postId: number;
}

export interface PostLikeCancelEntity {
  userId: string;
  postId: number;
}

export interface PostLikeCntEntity {
  userId: string;
}
